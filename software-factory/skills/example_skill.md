# Modern UI Requirements

When writing frontend code or designing interfaces, always follow these guidelines:

- Use clear semantic HTML5 tags instead of just generic `div`s.
- Keep the user interface clean with plenty of white space.
- Write accessible interfaces (include aria-labels, alt text, role attributes where necessary).
- Write descriptive comments for complex UI logic.
